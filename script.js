const input=document.getElementById("pw"),out=document.getElementById("content");function a(){input.focus({preventScroll:true})}window.addEventListener("load",()=>{document.title="";setTimeout(a,120)});document.body.addEventListener("click",a);document.body.addEventListener("touchstart",a,{passive:true});input.addEventListener("input",async()=>{let v=input.value.toUpperCase();input.value=v.slice(-12);for(const p of CONFIG.passwords){if(v.endsWith(p.toUpperCase())){const h=await fetch(CONFIG.contentFile).then(r=>r.text());out.innerHTML=h;out.classList.remove("hidden");requestAnimationFrame(()=>out.classList.add("show"));input.blur();return;}}});